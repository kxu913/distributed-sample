spring:

  cloud:
    nacos:
      discovery:
        service: gateway-service
        server-addr: 127.0.0.1:8848 #Nacos地址
      config:
#        namespace: dev
#        group: dev
        server-addr: 127.0.0.1:8848 #Nacos地址
        file-extension: yaml

